# 原始数据集 JSON 的版本边界

本目录是 2026-06-24 从观远 BI workshop 实例导出的历史快照，用于审计原数据集结构和平台资源 ID。

v1.4.1 已在逻辑层把误名的 `param_会员生命周期阈值` 拆成：

- `param_会员生命周期阈值`
- `param_门店会员占比预警阈值`

原始 JSON 仍保存旧资产名和旧字段，不能代表 v1.4.1 的参数结构。当前结构以 `数据集/结构定义/`、`数据集/数据样本/` 和 `清单/数据集清单.csv` 为准；平台资源 ID 不可移植。
